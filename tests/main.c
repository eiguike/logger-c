#include <stdio.h>
#include <stdlib.h>

void testBasic()
{
  // do some nice calculation; store the results in `foo` and `bar`,
  // respectively
  printf("HEUHEUHEU\n");
  int i = 1;
  i = 1/(i - 1);
}

void testAdvanced()
{
}

int main(int argc, char** argv)
{
  testBasic();
  testAdvanced();
}
